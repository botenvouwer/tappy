// Runner just runs an evaluation every x milliseconds to check if there are tabs that could be closed

const interval = setInterval(function() {

    //console.log("Test interval "+ new Date().toDateString()); //Check if I'm alive

}, 2000);

runner = interval;